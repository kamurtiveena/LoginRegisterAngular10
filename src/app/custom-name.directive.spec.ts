import { CustomNameDirective } from './custom-name.directive';

describe('CustomNameDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomNameDirective();
    expect(directive).toBeTruthy();
  });
});
